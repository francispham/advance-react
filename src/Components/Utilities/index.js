import { custom, custom1, lightGrey, grey, black, teal, purp } from "./colors";

import Mount from './Mount';
import Portal from './Portal';
import Toggle from './Toggle';

export * from './colors';

export { 
  Mount, Portal, Toggle, custom, custom1, lightGrey, grey, black, teal, purp
};
